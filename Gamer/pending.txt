Nedd to add update password 
 {activeContent === 'recommendations' && (
                <div className="row">
                  {merchant &&
                    merchant.map((product, index) => (
                      <div key={index} className="col-md-4 mb-3">
                        <Recommended
                          title={product.title}
                          desc={product.description}
                          brand={product.brand}
                          price={product.price}
                          className="w-100" // Adjust the width as per your requirements
                        />
                      </div>
                    ))}
                </div>
              )}